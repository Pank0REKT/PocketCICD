<Window x:Class="PocketCICD.ProjectNameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Название проекта"
        Height="200" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1E1E2E">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Введите название проекта:"
                   Foreground="#CDD6F4" FontFamily="Segoe UI" FontSize="13"/>

        <TextBox Grid.Row="2"
                 x:Name="TxtName"
                 Height="34" Padding="8,0"
                 FontFamily="Segoe UI" FontSize="13"
                 Foreground="#CDD6F4" Background="#313244"
                 BorderBrush="#45475A" BorderThickness="1"
                 VerticalContentAlignment="Center"
                 KeyDown="TxtName_KeyDown"/>

        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="Отмена" Height="32"
                    Foreground="#6C7086" Background="Transparent"
                    BorderBrush="#45475A" BorderThickness="1"
                    Cursor="Hand" Click="BtnCancel_Click"/>

            <Button Grid.Column="2" Content="Сохранить" Height="32"
                    Foreground="#A6E3A1" Background="Transparent"
                    BorderBrush="#A6E3A1" BorderThickness="1"
                    Cursor="Hand" Click="BtnOk_Click"/>
        </Grid>
    </Grid>
</Window>